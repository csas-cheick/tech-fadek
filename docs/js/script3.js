document.addEventListener('DOMContentLoaded', function () {
    const modalPFE = new bootstrap.Modal(document.getElementById('modalPFE'));
    const modalExpose = new bootstrap.Modal(document.getElementById('modalExpose'));
});
